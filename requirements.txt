# ==========================================================
# Dependências principais para o projeto "Gerador de Mapas de Calor para Otimização de Espaços Públicos"
# ==========================================================
# Instale estas dependências com: pip install -r requirements.txt
#
# Este arquivo lista apenas as dependências diretas do projeto com suas
# versões "congeladas" para garantir a reprodutibilidade. O pip se encarregará
# de instalar as sub-dependências compatíveis.
# ==========================================================

# A biblioteca principal da Ultralytics, que contém o YOLOv8
# e todas as suas dependências necessárias (como torch, torchvision, numpy).
ultralytics==8.2.2

# A biblioteca para criar a interface web interativa.
gradio==4.21.0

# A biblioteca fundamental para processamento de imagem e vídeo.
opencv-python==4.9.0.80

# Uma biblioteca para criar barras de progresso elegantes no terminal.
# Embora não seja visível na UI final do Gradio, é usada no backend
# para logar o progresso no console durante a execução.
tqdm==4.66.2